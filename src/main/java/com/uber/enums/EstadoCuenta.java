package com.uber.enums;

/**
 * Estados posibles de una cuenta de usuario en la aplicación.
 * Se usa para controlar si un usuario puede iniciar sesión o usar el servicio.
 */
public enum EstadoCuenta {
    ACTIVO,
    SUSPENDIDO,
    BLOQUEADO
}
